import React from "react";
import ReactDOM from "react-dom";

import Family from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <Family />
  </React.StrictMode>,
  rootElement
);
